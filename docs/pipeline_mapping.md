# Notebook-to-Pipeline Mapping

This document maps existing notebook cleaning and feature-engineering logic to the target modular pipeline under `src/tennis_pipeline/steps/`.

## Mapping table

| Notebook source (file + heading/comment anchor) | Operation currently performed in notebook | Target step module | Target function name | Input DataFrame expectation | Output DataFrame expectation |
|---|---|---|---|---|---|
| `checkpoint_1/main.ipynb` and `submissions/831004628_checkpoint1.ipynb` → code cell comment `#Data Pre-Processing, always assume these functions are done in-place` | Drop `Unnamed` columns, remove duplicates using match/event keys, coerce date columns, drop `.Percent` columns via `preprocess_raw_tennis` | `src/tennis_pipeline/steps/03_clean_values.py` | `clean_core_fields(df: pd.DataFrame) -> pd.DataFrame` | Raw merged match rows containing mixed dtypes and possible duplicate rows/unnamed columns | Same row grain (one row per match record), with normalized date dtypes, duplicates removed, and noisy helper columns removed |
| `submissions/831004628_checkpoint2.ipynb` → code cell comment `# Load and combine yearly ATP match files` (calls `preprocess_atp_matches`) | Apply standardized ATP cleanup after concat: trim column names, duplicate removal by ATP keys, numeric coercions for rank/race/set count fields | `src/tennis_pipeline/steps/03_clean_values.py` | `clean_atp_values(df: pd.DataFrame) -> pd.DataFrame` | Concatenated ATP yearly CSV rows with ATP-native column names (e.g., `tourney_id`, `tourney_date`, `winner_id`, `loser_id`) | Cleaned ATP table with consistent schema types and deduplicated match records |
| `submissions/831004628_checkpoint2.ipynb` → code cell comment `# Build target and leakage-safe pre-match features...` (block building paired numeric/categorical features) | Create mirrored team-side features (`team1_*`, `team2_*`) from `PlayerTeam1.*` / `PlayerTeam2.*`; build `diff_*` and `abs_diff_*`; create `same_*` for selected categorical fields | `src/tennis_pipeline/steps/05_build_features_static.py` | `build_paired_player_features(df: pd.DataFrame) -> pd.DataFrame` | Clean table with both player-side columns present for selected suffix sets (rank/points/demographic/handedness/country) | Feature-enriched table retaining original rows plus engineered paired numeric/categorical columns |
| `submissions/831004628_checkpoint2.ipynb` → same feature-building cell, comments `# Maintain backwards-compatible names...` and `# Keep rows where the core pre-match rank signal is available.` | **Rank/race diff features**: alias `rank_diff`, `abs_rank_diff` from `diff_sglrollrank`; alias `race_rank_diff`, `abs_race_rank_diff` from `diff_sglracerank`; enforce non-null `rank_diff` rows | `src/tennis_pipeline/steps/05_build_features_static.py` | `add_rank_and_race_diff_features(df: pd.DataFrame) -> pd.DataFrame` | DataFrame containing `PlayerTeam1.SglRollRank`/`PlayerTeam2.SglRollRank`; optionally race rank columns | DataFrame with canonical rank/race diff columns for modeling (`rank_diff`, `race_rank_diff`, and abs variants), filtered to rows with usable core rank signal |
| `submissions/831004628_checkpoint2.ipynb` → same feature-building cell (`meta_feature_candidates`) and cell `# Surface-level anomaly detection slice for RQ2` (`surface_candidates = ["CourtSurface", "Court.Surface", "Court"]`) | **Surface/court handling**: resolve available surface/court source column, fill missing with `"Unknown"`, one-hot encode for modeling/anomaly slice | `src/tennis_pipeline/steps/05_build_features_static.py` | `normalize_surface_context(df: pd.DataFrame) -> pd.DataFrame` | DataFrame with at least one optional surface/court field among `CourtSurface`, `Court.Surface`, `Court` | DataFrame with a canonical surface context column (e.g., `surface_context`) and consistent missing-value policy ready for downstream encoding |
| `submissions/831004628_checkpoint2.ipynb` → same feature-building cell comment `# Binary target: whether Team1 won the match.` | Compute label `team1_wins = (WinningPlayerId == PlayerTeam1.PlayerId).astype(int)` after team-side random swap logic | `src/tennis_pipeline/steps/04_split_roles.py` | `assign_team_roles_and_target(df: pd.DataFrame, random_seed: int) -> pd.DataFrame` | Pre-cleaned match rows containing `WinningPlayerId`, `PlayerTeam1.PlayerId`, `PlayerTeam2.PlayerId` and mirrored player-side attributes | Role-consistent table where Team1/Team2 columns remain aligned post-swap, with binary supervised target column `team1_wins` |
| `submissions/831004628_checkpoint2.ipynb` → code cell comment `# External method prototype: Elo feature engineering and incremental evaluation` | **Elo feature creation logic**: sort chronologically, initialize per-player ratings, compute expected score with logistic transform, append pre-match `elo_diff_team1`, then update ratings online per observed result | `src/tennis_pipeline/steps/06_build_features_temporal_elo.py` | `add_elo_features(df: pd.DataFrame, base_rating: int = 1500, k_factor: int = 24, date_col: str = "StartDate") -> pd.DataFrame` | DataFrame with match chronology (`StartDate` preferred) plus IDs `WinningPlayerId`, `PlayerTeam1.PlayerId`, `PlayerTeam2.PlayerId`; one row per match in temporal order | Same row count/order as sorted input with temporal pre-match Elo features (at minimum `elo_diff_team1`) added leakage-safely (updates applied only after current-row feature capture) |
| `submissions/831004628_checkpoint2.ipynb` → feature cell subset selection (`engineered_features`, `meta_feature_candidates`) and Elo cell `external_features` | Select final modeling features from engineered numeric diffs + context fields, preserving only pre-match-safe fields | `src/tennis_pipeline/steps/07_finalize_model_table.py` | `select_model_features(df: pd.DataFrame) -> pd.DataFrame` | Feature-rich frame containing engineered diffs, optional Elo, optional surface/round metadata, and target | Final model table with consistent feature schema and target column for training/evaluation |

## Explicit coverage notes

- **Rank/race diff features** are sourced from the checkpoint 2 feature engineering cell that computes `diff_sglrollrank`, `diff_sglracerank` and backfills canonical aliases (`rank_diff`, `race_rank_diff`).
- **Surface/court handling** is sourced from both the static feature candidate list and the RQ2 anomaly slice where the notebook falls back across `CourtSurface`, `Court.Surface`, then `Court`, with `Unknown` imputation.
- **Elo feature creation logic** is sourced from the external-method cell implementing chronological, per-player online updates with `expected_score` and pre-update capture of `elo_diff_team1`.
